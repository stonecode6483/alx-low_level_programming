#include "main.h"
/**
* _islower - a function to check loweer case.
* @c: character to chek
* Return: 1 if c is lower case,otherwise return 0
*/
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
